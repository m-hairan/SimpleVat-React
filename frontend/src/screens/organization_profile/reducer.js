import { ORGANIZATION_PROFILE } from 'constants/types'

const initState = {
}

const OrganizationProfileReducer = (state = initState, action) => {
  const { type, payload} = action
  
  switch(type) {

    default:
      return state
  }
}

export default OrganizationProfileReducer